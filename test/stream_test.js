let assert = require("assert");
describe("stream", () => {
  describe("#master", () => {
    //model workers
    it("on message close - exit properly", () => {
      assert(true);
    });
    it("on worker exit - restart worker", () => {
      assert(true);
    });
    it("on worker message - handle message", () => {
      assert(true);
    });
    it("on its own exit - restart (?)", () => {
      assert(true);
    });
  });
});
